# 🚀 ขั้นตอนง่ายสุด (5 ขั้นเท่านั้น!)

เก็บเฉพาะที่จำเป็น ไม่มีความซับซ้อน

---

## ขั้นตอนที่ 1: เตรียม

**ต้องทำ**:
- [ ] สร้างบัญชี GitHub ที่ https://github.com
- [ ] ติดตั้ง Git จาก https://git-scm.com/download/win

**เสร็จแล้ว?** ไปขั้นตอนที่ 2

---

## ขั้นตอนที่ 2: สร้าง Repository บน GitHub

**ไป https://github.com**

**ทำนี้**:
1. คลิก ➕ (มุมขวาบน)
2. เลือก "New repository"
3. ใส่ชื่อ: **KPI-Sales-Dashboard**
4. เลือก **Public**
5. คลิก **"Create repository"**

**✅ เสร็จแล้ว**

---

## ขั้นตอนที่ 3: ตั้งค่า PowerShell

**เปิด PowerShell** ที่โฟลเดอร์ `D:\VS CODE`

(คลิกขวา → Open PowerShell here)

**พิมพ์**:
```powershell
git config --global user.name "ชื่อของคุณ"
git config --global user.email "อีเมลของคุณ"
```

**ตัวอย่าง**:
```powershell
git config --global user.name "Somchai Samart"
git config --global user.email "somchai@gmail.com"
```

**ไม่มี error = ปกติ ✅**

---

## ขั้นตอนที่ 4: อัพโหลด (ทำเรื่อย ๆ)

**ยังอยู่ใน PowerShell ที่ `D:\VS CODE`**

**พิมพ์ทีละคำสั่ง** (รอให้จบแต่ละคำสั่ง):

### คำสั่ง 1:
```powershell
git init
```

### คำสั่ง 2:
```powershell
git add .
```

### คำสั่ง 3:
```powershell
git commit -m "First upload"
```

### คำสั่ง 4:
```powershell
git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
```

⚠️ **แทน** `YOUR_USERNAME` **ด้วยชื่อ GitHub จริงของคุณ**

**ตัวอย่าง**:
```powershell
git remote add origin https://github.com/somchai99/KPI-Sales-Dashboard.git
```

### คำสั่ง 5:
```powershell
git branch -M main
```

### คำสั่ง 6:
```powershell
git push -u origin main
```

**✅ เสร็จแล้ว!**

---

## ขั้นตอนที่ 5: ตรวจสอบ

**ไปที่ GitHub**: https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard

**ต้องเห็น**:
- index.html ✅
- styles.css ✅
- script.js ✅
- README.md ✅

**ถ้าเห็นหมด = สำเร็จ!** 🎉

---

## ❌ ถ้าเกิด Error

### Error: "fatal: not a git repository"
**ทำ**:
```powershell
git init
```

### Error: "fatal: Authentication failed"
**ทำ**:
1. ตรวจสอบชื่อ GitHub ว่าถูกต้อง
2. ตรวจสอบ URL ว่าถูกต้อง

### Error: อื่น ๆ
**ส่ง Screenshot PowerShell มาให้ผมดู**

---

## 📝 สรุป

```
1. สร้างบัญชี GitHub + ติดตั้ง Git
   ↓
2. สร้าง Repository
   ↓
3. ตั้งค่า PowerShell
   ↓
4. พิมพ์คำสั่ง 6 ข้อ
   ↓
5. ตรวจสอบ GitHub
   ↓
✅ เสร็จ!
```

---

**เพิ่มเติม**: ถ้ายังงง ส่ง PowerShell Output + Screenshot มาให้ผมดู

**Happy Coding! 🚀**
