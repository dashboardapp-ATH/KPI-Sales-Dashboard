# 📚 GitHub สำหรับมือใหม่ (อธิบายอย่างง่าย)

สอนแบบเด็กป.4 ให้เข้าใจ GitHub โดยไม่งง!

---

## 🎯 GitHub คืออะไร?

**ลองนึกตามนี้**:
- 📁 **Google Drive** = เก็บไฟล์คลาวด์
- 📁 **GitHub** = เก็บโค้ดและติดตามการเปลี่ยนแปลง

**ตัวอย่างประจำวัน**:
- ถ้าคุณเขียน Word แล้วบันทึกเป็น "เอกสาร v1", "เอกสาร v2", "เอกสาร v3" 
- GitHub ทำให้คุณไม่ต้องทำแบบนี้ - มันจำเวอร์ชั่นเองให้

---

## 🚀 เริ่มต้นก่อน - สิ่งที่ต้องมี

### ✅ 1. สร้างบัญชี GitHub

**ขั้นตอน**:
1. เปิดเว็บ https://github.com
2. มองหาปุ่ม "Sign up" (มุมขวาบน)
3. ใส่ Email ของคุณ
4. ตั้ง Password ให้ยาว (ต้องมี ตัวอักษร ตัวเลข)
5. ใส่ Username (ชื่อบน GitHub - เช่น "john-sales-dashboard")
6. ตั้ง Public (ให้คนอื่นเห็นได้)
7. คลิก "Create account"

**ตรวจสอบ Email**: GitHub จะส่ง Email มาให้ยืนยัน คลิกลิงก์ในนั้น

✅ **เสร็จแล้ว! คุณมีบัญชี GitHub**

---

### ✅ 2. ติดตั้ง Git (โปรแกรมที่ใช้อัพโหลด)

**Git คืออะไร?**
- GitHub = เซิร์ฟเวอร์เก็บโค้ด (บน internet)
- Git = โปรแกรมที่ใช้ส่งโค้ดไป GitHub

**วิธีติดตั้ง (Windows)**:

1. เปิด https://git-scm.com/download/win
2. ดาวน์โหลด (จะมี installer ขนาด ~50MB)
3. ดับเบิลคลิก installer
4. กดปุ่ม "Next" เรื่อย ๆ ไป (ใช้ค่า default ได้)
5. กดปุ่ม "Install"
6. กดปุ่ม "Finish"

**วิธีตรวจสอบ**:
1. เปิด PowerShell (ค้นหา PowerShell บน Windows)
2. พิมพ์: `git --version`
3. ถ้าเห็น "git version 2.xxx" = ติดตั้งสำเร็จ ✅

---

## 🏠 ขั้นตอนที่ 1: สร้าง Repository บน GitHub

**Repository** = โฟลเดอร์เก็บโปรเจ็กต์บน GitHub

### วิธีสร้าง:

1. **เข้า GitHub** → https://github.com
2. **เข้าสู่ระบบ** ด้วยบัญชีของคุณ
3. **มองหาปุ่ม "+"** (มุมขวาบน)
4. **คลิกแล้วเลือก "New repository"**

```
┌─────────────────────────┐
│     GitHub Home         │
├─────────────────────────┤
│  + ▼  (ปุ่ม New)         │
│  ▼ New repository       │
│  New gist              │
└─────────────────────────┘
```

5. **ในหน้า "Create a new repository"** ให้กรอก:

```
┌────────────────────────────────────────────┐
│ Repository name *                          │
│ KPI-Sales-Dashboard                        │
│                                            │
│ Description                                │
│ KPI Sales Dashboard - Web App              │
│                                            │
│ ○ Public  (เลือกแบบนี้)                   │
│ ○ Private                                  │
│                                            │
│ ☐ Add a README file                       │
│ ☐ Add .gitignore                          │
│ ☐ Choose a license                        │
│                                            │
│         [Create repository] ปุ่มสีเขียว     │
└────────────────────────────────────────────┘
```

**อธิบายแต่ละช่อง**:
- **Repository name**: ชื่อโปรเจ็กต์ (เช่น `KPI-Sales-Dashboard`)
- **Description**: คำอธิบายสั้น ๆ (ไม่บังคับ แต่ดูดี)
- **Public**: ให้คนอื่นเห็นได้ (เลือกแบบนี้ถ้าต้องการแชร์)
- **ไม่ต้องเลือก** "Add a README", ".gitignore", "license" เพราะมีไฟล์เหล่านี้แล้ว

6. **คลิก "Create repository"** (ปุ่มสีเขียว)

✅ **เสร็จแล้ว! Repository ถูกสร้าง**

---

## 📂 ขั้นตอนที่ 2: ทำให้ GitHub รู้จักโฟลเดอร์ของคุณ

**ตอนนี้**: โปรเจ็กต์อยู่บนเครื่องของคุณ (`D:\VS CODE`)
**ที่ต้องทำ**: บอก Git ให้ติดตาม folder นี้

### วิธีทำ:

**ขั้นที่ 1: เปิด PowerShell**

```
Windows Key + R
Type: powershell
Press: Enter
```

จะเห็นหน้าต่างสีดำแบบนี้:

```
PS C:\Users\YourName>
```

**ขั้นที่ 2: ไปที่โฟลเดอร์ของคุณ**

พิมพ์:
```powershell
cd D:\VS\ CODE
```

(หรือถ้าเป็น path อื่น ก็ใส่ path นั้น)

ตรวจสอบ: ควรเห็น
```
PS D:\VS CODE>
```

**ขั้นที่ 3: ตั้งค่า Git**

พิมพ์:
```powershell
git config --global user.name "Your Full Name"
git config --global user.email "your.email@gmail.com"
```

**ตัวอย่าง**:
```powershell
git config --global user.name "Somchai Samart"
git config --global user.email "somchai@gmail.com"
```

โปรแกรมไม่มีคำตอบ ลองพิมพ์คำสั่งต่อไป (ปกติแบบนี้)

---

## 📤 ขั้นตอนที่ 3: อัพโหลด (Push) โค้ดขึ้น GitHub

**ตอนนี้ยังอยู่ใน PowerShell ที่ `D:\VS CODE`**

### วิธีทำ: 4 ขั้นง่าย

**ขั้นที่ 1: บอก Git ให้เริ่มติดตาม**

พิมพ์:
```powershell
git init
```

**จะเห็น**:
```
Initialized empty Git repository in D:\VS CODE\.git/
```

ความหมาย: Git พร้อมแล้ว

---

**ขั้นที่ 2: บอก Git ว่าไฟล์ไหนที่จะอัพโหลด**

พิมพ์:
```powershell
git add .
```

(จุด `.` หมายถึง "ไฟล์ทั้งหมด")

---

**ขั้นที่ 3: บันทึกการเปลี่ยนแปลง**

พิมพ์:
```powershell
git commit -m "Initial commit: KPI Sales Dashboard"
```

**จะเห็น**:
```
[master (root-commit) abc1234] Initial commit: KPI Sales Dashboard
 7 files changed, 500 insertions(+)
```

ความหมาย: บันทึกสำเร็จแล้ว

---

**ขั้นที่ 4: เชื่อมต่อกับ GitHub**

1. **ไปที่ GitHub Repository ของคุณ**
   - https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard

2. **คลิกปุ่มสีเขียว "Code"** (บนหน้า Repository)

3. **คัดลอก URL** (HTTPS)
   ```
   https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
   ```

4. **กลับมาที่ PowerShell** แล้วพิมพ์:
   ```powershell
   git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
   ```
   
   (แทน `YOUR_USERNAME` ด้วยชื่อ GitHub จริงของคุณ)

---

**ขั้นที่ 5: อัพโหลดขึ้น GitHub**

พิมพ์:
```powershell
git branch -M main
git push -u origin main
```

**จะเห็น**:
```
Enumerating objects: 7, done.
Counting objects: 100% (7/7), done.
Delta compression using up to 8 threads
Compressing objects: 100% (6/6), done.
Writing objects: 100% (7/7), 1.23 KiB | 1.23 MiB/s, done.
Total 7 (delta 0), reused 0 (delta 0), pack-reused 0
remote: Create a pull request for 'main' on GitHub by visiting:
remote:   https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard/pull/new/main
To https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

✅ **เสร็จแล้ว! โค้ดอยู่บน GitHub**

---

## ✅ ตรวจสอบว่าสำเร็จ

1. **เข้า GitHub** → https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard
2. **ตรวจสอบไฟล์**:
   - เห็น `index.html` ✅
   - เห็น `styles.css` ✅
   - เห็น `script.js` ✅
   - เห็น `README.md` ✅

ถ้าเห็นไฟล์ทั้งหมด = **สำเร็จ!** 🎉

---

## 🔗 แชร์กับคนอื่น

ตอนนี้คุณสามารถแชร์ได้ 2 วิธี:

### วิธี 1: ให้คนอื่นดาวน์โหลด

บอกให้คนอื่นไป: `https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard`

ขั้นตอน:
1. คลิก **"Code"** (สีเขียว)
2. คลิก **"Download ZIP"**
3. แตกไฟล์
4. เปิด `index.html` ในเบราว์เซอร์

### วิธี 2: ให้คนอื่น Clone (สำหรับนักพัฒนา)

คนอื่นพิมพ์ใน PowerShell:
```powershell
git clone https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
```

Git จะดาวน์โหลดโปรเจ็กต์ทั้งหมด

---

## 🔄 เมื่อคุณแก้ไขโค้ด

ถ้าคุณเพิ่มฟีเจอร์ใหม่หรือแก้บั๊ก ต้องอัพโหลดใหม่:

**ใน PowerShell (ที่โฟลเดอร์โปรเจ็กต์)**:

```powershell
# ดูว่าเปลี่ยนแปลงอะไร
git status

# เพิ่มไฟล์ที่เปลี่ยน
git add .

# บันทึก
git commit -m "Feat: อธิบายสิ่งที่เปลี่ยน"

# อัพโหลด
git push origin main
```

---

## 🆘 ถ้าเกิดปัญหา

### ❌ "fatal: not a git repository"

**ความหมาย**: ลืมทำ `git init`

**วิธีแก้**:
```powershell
git init
git add .
git commit -m "Initial commit"
```

---

### ❌ "fatal: Authentication failed"

**ความหมาย**: ชื่อ User หรือ Password ผิด

**วิธีแก้**: 
1. ตรวจสอบ URL ใน GitHub (คัดลอกใหม่)
2. ใส่ URL ใหม่:
   ```powershell
   git remote set-url origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
   ```

---

### ❌ "fatal: remote origin already exists"

**ความหมาย**: ลืมว่าเชื่อมต่อแล้ว

**วิธีแก้**:
```powershell
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
```

---

## 📝 สรุปคำสั่งสำคัญ

| คำสั่ง | ความหมาย |
|--------|---------|
| `git init` | เริ่มต้น Git |
| `git add .` | เลือกไฟล์ที่จะอัพโหลด |
| `git commit -m "..."` | บันทึกการเปลี่ยนแปลง |
| `git remote add origin URL` | เชื่อมต่อ GitHub |
| `git push origin main` | อัพโหลดขึ้น GitHub |
| `git status` | ดูสถานะ |
| `git log` | ดูประวัติ |

---

## 🎓 ตัวอย่าง Full Workflow

```powershell
# 1. ไปที่โฟลเดอร์
cd D:\VS CODE

# 2. เริ่มต้น Git
git init

# 3. ตั้งค่า Username
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

# 4. เพิ่มไฟล์
git add .

# 5. บันทึก
git commit -m "Initial commit: KPI Sales Dashboard"

# 6. เชื่อมต่อ GitHub
git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git

# 7. เปลี่ยน branch
git branch -M main

# 8. อัพโหลด
git push -u origin main
```

---

## ✨ เสร็จแล้ว!

ตอนนี้:
✅ โปรเจ็กต์อยู่บน GitHub
✅ คุณสามารถแชร์ได้
✅ คุณสามารถแก้ไขและอัพโหลดใหม่ได้
✅ ผู้คนอื่นสามารถ Fork และ Contribute ได้

---

## 🎯 ขั้นตอนต่อไป

- 📖 อ่าน README.md เพื่อรายละเอียดเพิ่มเติม
- 🤝 อ่าน CONTRIBUTING.md เพื่อให้คนอื่น Contribute
- 🌐 ตั้งค่า GitHub Pages เพื่อให้ใช้ Online (ถ้าต้องการ)

---

**Good luck! 🚀 ถ้ามีปัญหา ให้บอกผม**

**Last Updated**: 5 กุมภาพันธ์ 2026
