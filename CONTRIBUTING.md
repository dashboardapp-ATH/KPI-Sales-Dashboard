# 🤝 วิธีการมีส่วนร่วมในการพัฒนา

ขอบคุณที่สนใจพัฒนา KPI Sales Dashboard! ไฟล์นี้บอกวิธีการสนับสนุนโครงการ

---

## 📋 สารบัญ

1. [Code of Conduct](#code-of-conduct)
2. [วิธีเริ่มต้น](#วิธีเริ่มต้น)
3. [ขั้นตอนการมีส่วนร่วม](#ขั้นตอนการมีส่วนร่วม)
4. [Style Guide](#style-guide)
5. [Pull Request Process](#pull-request-process)

---

## Code of Conduct

ทุกคนต้องปฏิบัติต่อกันด้วยความเคารพและสุภาพ:

- ✅ ใจเปิด ยอมรับความเห็นต่างและคำวิจารณ์
- ✅ มีความสุภาพ ใช้ภาษาที่เหมาะสม
- ✅ เข้าใจกัน ลองเข้าใจดำเนินการของผู้อื่น
- ❌ ไม่ล่วงละเมิด ไม่มีการคุกคาม
- ❌ ไม่กระตุ้น ไม่มีการเสียดสี หรือประชดประชัน

---

## วิธีเริ่มต้น

### ขั้นตอนที่ 1: ตั้งค่า Environment

```bash
# Clone repository
git clone https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
cd KPI-Sales-Dashboard

# สร้าง branch สำหรับการพัฒนา
git checkout -b feature/your-feature-name
```

### ขั้นตอนที่ 2: ติดตั้ง

เนื่องจากเป็น Static HTML ไม่ต้องติดตั้งอะไร แค่เปิด `index.html` ในเบราว์เซอร์

(ถ้าต้องการใช้ Local Server)
```bash
python -m http.server 8000
# หรือ
npx http-server
```

### ขั้นตอนที่ 3: ตรวจสอบเวอร์ชันของคุณ

```bash
git --version
node --version  # ถ้าต้องการใช้ Node tools
```

---

## ขั้นตอนการมีส่วนร่วม

### ประเภทการมีส่วนร่วม

#### 1. 🐛 การแก้จุดบกพร่อง (Bug Fix)

**ขั้นตอน**:

```bash
# 1. สร้าง issue ใหม่ (ถ้ายังไม่มี)
# ไปที่ Issues tab → New Issue

# 2. สร้าง branch สำหรับแก้ไข
git checkout -b bugfix/issue-number

# 3. แก้ไขโค้ด
# (แก้ไขไฟล์ที่จำเป็น)

# 4. ทดสอบการแก้ไข
# เปิด index.html ในเบราว์เซอร์ทดสอบ

# 5. Commit และ Push
git add .
git commit -m "Fix: คำอธิบายปัญหาและวิธีแก้"
git push origin bugfix/issue-number
```

#### 2. ✨ การเพิ่มฟีเจอร์ใหม่ (Feature)

**ขั้นตอน**:

```bash
# 1. สร้าง issue เพื่อหารือ feature ใหม่
# (ไม่ต้องรอไปทำเลย ลองหารือก่อน)

# 2. สร้าง branch สำหรับ feature
git checkout -b feature/feature-name

# 3. เพิ่มฟีเจอร์ใหม่
# (แก้ไขไฟล์)

# 4. ทดสอบให้แน่ใจว่าทำงานได้ดี
# เปิด index.html ในเบราว์เซอร์

# 5. Commit และ Push
git add .
git commit -m "Feat: คำอธิบาย feature ใหม่"
git push origin feature/feature-name

# 6. สร้าง Pull Request
```

#### 3. 📚 ปรับปรุงเอกสาร (Documentation)

```bash
git checkout -b docs/improvement-name

# แก้ไข README.md หรือไฟล์เอกสารอื่น

git add .
git commit -m "Docs: ปรับปรุงคำอธิบาย"
git push origin docs/improvement-name
```

#### 4. 🎨 ปรับปรุงการออกแบบ (UI/UX)

```bash
git checkout -b design/improvement-name

# แก้ไข styles.css หรือ HTML

git add .
git commit -m "Design: ปรับปรุงส่วน design"
git push origin design/improvement-name
```

---

## Style Guide

### HTML

```html
<!-- ✅ ดี -->
<button class="btn btn-primary" onclick="filterByProduct('all', this)">
    📊 ทั้งหมด
</button>

<!-- ❌ ไม่ดี -->
<button onclick="filter()">Filter</button>
```

**กฎ**:
- ใช้ class names ที่เข้าใจง่าย
- ใช้ semantic HTML tags (header, section, main, etc.)
- เพิ่ม comments สำหรับส่วนที่ซับซ้อน

### CSS

```css
/* ✅ ดี */
:root {
    --primary-color: #2563eb;
    --text-dark: #1e293b;
}

.btn {
    padding: 10px 20px;
    border-radius: 8px;
    transition: all 0.3s ease;
}

/* ❌ ไม่ดี */
.btn {
    padding: 10px;
    color: blue;
}
```

**กฎ**:
- ใช้ CSS Variables สำหรับสีและขนาด
- ใช้ class names ที่มีความหมาย (BEM convention)
- เพิ่ม media queries สำหรับ responsive design

### JavaScript

```javascript
// ✅ ดี
function filterByProduct(product, element) {
    selectedProduct = product;
    
    // ล้าง active state ของ items ทั้งหมด
    document.querySelectorAll('.sidebar-item').forEach(btn => {
        btn.classList.remove('active');
    });
    
    element.classList.add('active');
    applyFilter();
}

// ❌ ไม่ดี
function filter(p, e) {
    // ...
}
```

**กฎ**:
- ใช้ function names ที่สั้นแต่มีความหมาย
- เพิ่ม comments สำหรับลอจิก
- ใช้ const/let แทน var
- ใช้ arrow function เมื่อเหมาะสม

---

## Commit Message Convention

ใช้ format ต่อไปนี้:

```
<type>: <subject>

<body>

<footer>
```

### Type:
- `feat`: ฟีเจอร์ใหม่
- `fix`: การแก้จุดบกพร่อง
- `docs`: เอกสาร
- `style`: การจัดรูปแบบ (ไม่เปลี่ยนลอจิก)
- `refactor`: โค้ดใหม่โดยไม่เปลี่ยนฟังก์ชันนาลิตี
- `test`: เพิ่มการทดสอบ
- `chore`: อัพเดท dependencies

### ตัวอย่าง:

```
feat: เพิ่มการส่งออก CSV

- เพิ่มฟังก์ชัน exportToCSV()
- อัพเดท UI สำหรับปุ่ม export
- เพิ่ม XLSX library

Closes #123
```

---

## Pull Request Process

### ขั้นตอน 1: ตรวจสอบก่อน Submit

```bash
# สรุป commit ของคุณ
git log origin/main..HEAD

# ตรวจสอบการเปลี่ยนแปลง
git diff origin/main
```

### ขั้นตอน 2: สร้าง Pull Request

ใน GitHub:

1. ไปที่ "Pull requests" tab
2. คลิก "New pull request"
3. เลือก branch ของคุณ
4. คลิก "Create pull request"

### ขั้นตอน 3: Fill PR Template

```markdown
## 📝 คำอธิบาย
ขอให้อธิบายการเปลี่ยนแปลงของคุณโดยสังเขป

## 🎯 ประเภท
- [x] Bug fix
- [ ] Feature
- [ ] Documentation update

## 📋 Checklist
- [x] โค้ดได้ทำการ self-review
- [x] ทดสอบในเบราว์เซอร์หลักๆ
- [x] ไม่มี syntax errors
- [x] เพิ่มคำอธิบาย/comments หากจำเป็น
- [x] ไม่มี breaking changes

## 📸 Screenshots (ถ้ามีการเปลี่ยน UI)
[อัพโหลด screenshot]

## 🔗 Related Issues
Closes #123
```

### ขั้นตอน 4: รอการ Review

ผู้ดูแลจะตรวจสอบและคอมเมนต์ อาจขอการปรับปรุง:

- 📝 อัพเดท commit message
- 🎨 ปรับปรุง code style
- 🧪 เพิ่มการทดสอบ

### ขั้นตอน 5: Merge

เมื่อได้รับอนุมัติ PR จะถูก merge เข้า main branch

---

## 🧪 Testing

แม้ว่าไม่มี automated tests แต่ต้องทดสอบด้วยตนเอง:

### Desktop Testing

```
[ ] ทดสอบใน Chrome/Edge
[ ] ทดสอบใน Firefox
[ ] ทดสอบใน Safari
[ ] ตรวจสอบ Console สำหรับ errors
```

### Mobile Testing

```
[ ] ทดสอบใน iOS Safari
[ ] ทดสอบใน Chrome Mobile
[ ] ตรวจสอบ responsive layout
[ ] ทดสอบ sidebar toggle
```

### Feature Testing

```
[ ] ทดสอบ filter by date
[ ] ทดสอบ filter by product
[ ] ทดสอบ search functionality
[ ] ทดสอบ export Excel
[ ] ทดสอบ export PDF
[ ] ทดสอบ sidebar selection
```

---

## 🎓 ตัวอย่าง Pull Request

### Example 1: Bug Fix

```
Title: Fix chart not updating on product filter

Description:
- When clicking a product in sidebar, chart wasn't updating
- Root cause: applyFilter() wasn't calling updateCharts()
- Solution: Added updateCharts() call in filterByProduct()

Changes:
- Modified: script.js (line 145-150)

Testing:
- Tested on Chrome, Firefox
- Verified chart updates after product selection
```

### Example 2: New Feature

```
Title: Add Dark Mode toggle

Description:
- Added dark mode theme as requested in #45
- Users can toggle between light and dark mode
- Preference is saved to localStorage

Changes:
- Added: darkMode.css (new file)
- Modified: index.html (added toggle button)
- Modified: script.js (added dark mode logic)

Testing:
- Tested on Desktop and Mobile
- Verified theme persists after reload
```

---

## 🏆 Contribution Tips

### ✅ การประกอบการดีๆ

- 📖 อ่าน README และ existing code ก่อนเริ่ม
- 💬 หารือ feature ใหญ่ๆ ใน issue ก่อน
- 🎯 โฟกัสต่อเนื่องงานเดียว ต่อ PR
- 🧪 ทดสอบให้แน่ใจว่าไม่มี side effects
- 📝 เขียน meaningful commit messages
- 🙏 ยอมรับคำวิจารณ์ด้วยใจ

### ❌ สิ่งที่ไม่ควรทำ

- ❌ ไม่เปิด PR เพื่อ add dependencies โดยไม่หารือ
- ❌ ไม่ปลี่ยน repository structure ก่อนปรึกษา
- ❌ ไม่รวม unrelated changes ใน 1 PR
- ❌ ไม่ลบไฟล์ที่มีการใช้งาน

---

## 🎉 ขอบคุณ!

ขอบคุณมากที่ช่วยพัฒนา KPI Sales Dashboard! 

**Happy Contributing! 🚀**

---

**Last Updated**: 5 กุมภาพันธ์ 2026
