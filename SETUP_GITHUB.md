# 🚀 ขั้นตอนการอัพโหลดไป GitHub

คู่มือนี้อธิบายวิธีการสร้าง GitHub Repository และอัพโหลดโปรเจ็กต์

---

## 📋 สารบัญ

1. [ก่อนเริ่มต้น](#ก่อนเริ่มต้น)
2. [ขั้นตอนการสร้าง Repository](#ขั้นตอนการสร้าง-repository)
3. [ขั้นตอนการอัพโหลด](#ขั้นตอนการอัพโหลด)
4. [ยืนยันการสำเร็จ](#ยืนยันการสำเร็จ)
5. [การแชร์กับคนอื่น](#การแชร์กับคนอื่น)

---

## ก่อนเริ่มต้น

### ✅ ตรวจสอบสิ่งที่ต้องมี

- [ ] บัญชี GitHub (สร้างได้ที่ https://github.com/signup)
- [ ] Git ติดตั้งแล้ว (ตรวจสอบด้วย `git --version`)
- [ ] โฟลเดอร์โปรเจ็กต์พร้อม:
  - [x] index.html
  - [x] styles.css
  - [x] script.js
  - [x] README.md
  - [x] CONTRIBUTING.md
  - [x] LICENSE
  - [x] .gitignore

### ✅ ตั้งค่า Git (ครั้งแรก)

เปิด Terminal/PowerShell และรัน:

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

ตรวจสอบการตั้งค่า:

```bash
git config --global --list
```

---

## ขั้นตอนการสร้าง Repository

### 1️⃣ สร้าง Repository บน GitHub

**ขั้นตอน**:

1. ไปที่ https://github.com/new
2. เข้าสู่ระบบด้วยบัญชี GitHub ของคุณ
3. กรอกข้อมูล:

| ฟิลด์ | ค่า |
|------|-----|
| **Repository name** | `KPI-Sales-Dashboard` |
| **Description** | `📊 KPI Sales Dashboard - Web Application for Sales Reporting` |
| **Public/Private** | `Public` (เพื่อให้คนอื่นใช้ได้) |
| **Add a README file** | ❌ ไม่ต้อง (มีแล้ว) |
| **Add .gitignore** | ❌ ไม่ต้อง (มีแล้ว) |
| **Choose a license** | ❌ ไม่ต้อง (มี LICENSE ไฟล์แล้ว) |

4. คลิก **"Create repository"**

### 2️⃣ คัดลอก URL Repository

บนหน้า Repository ใหม่:

1. คลิก **"Code"** (ปุ่มสีเขียว)
2. เลือก **HTTPS**
3. คัดลอก URL (ตัวอย่าง):
   ```
   https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
   ```

---

## ขั้นตอนการอัพโหลด

### 3️⃣ เปิด Terminal ในโฟลเดอร์โปรเจ็กต์

**Windows (PowerShell)**:
```bash
# เปิด PowerShell
# ไปที่โฟลเดอร์
cd D:\VS CODE
```

**Mac/Linux**:
```bash
cd /path/to/KPI-Sales-Dashboard
```

### 4️⃣ เริ่มต้น Git Repository ในเครื่องของคุณ

```bash
# เริ่มต้น git repository
git init

# เพิ่มไฟล์ทั้งหมด
git add .

# ยืนยันการเปลี่ยนแปลงครั้งแรก
git commit -m "Initial commit: KPI Sales Dashboard"
```

### 5️⃣ เชื่อมต่อกับ GitHub

```bash
# เพิ่ม remote repository
git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git

# ตรวจสอบว่าเชื่อมต่อแล้ว
git remote -v
```

**คำอธิบาย**:
- `origin` = ชื่อของ remote repository
- URL = ที่อยู่บน GitHub

### 6️⃣ อัพโหลดข้อมูล (Push)

```bash
# เปลี่ยน branch เป็น main (ถ้ายัง)
git branch -M main

# อัพโหลดไป GitHub
git push -u origin main
```

**ตัวอักษร**:
- `-u` = set upstream (จำ branch สำหรับครั้งต่อไป)
- `origin` = remote repository
- `main` = branch name

### ⏳ รอการอัพโหลด

กระบวนการอาจใช้เวลา 5-30 วินาที ขึ้นอยู่กับ:
- ความเร็ว Internet
- ขนาดไฟล์

ตัวอย่าง Output ที่สำเร็จ:

```
Enumerating objects: 7, done.
Counting objects: 100% (7/7), done.
Delta compression using up to 8 threads
Compressing objects: 100% (6/6), done.
Writing objects: 100% (7/7), 1.23 KiB | 1.23 MiB/s, done.
Total 7 (delta 0), reused 0 (delta 0)
To https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

---

## ยืนยันการสำเร็จ

### ✅ ตรวจสอบบน GitHub

1. เข้าไปที่ https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard
2. ตรวจสอบไฟล์:
   - [x] index.html
   - [x] styles.css
   - [x] script.js
   - [x] README.md
   - [x] CONTRIBUTING.md
   - [x] LICENSE
   - [x] .gitignore

3. ตรวจสอบ README แสดงขึ้นมา

### ✅ ทดสอบการดาวน์โหลด

```bash
# สร้างโฟลเดอร์ใหม่
mkdir test-repo
cd test-repo

# Clone จาก GitHub
git clone https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git

# ตรวจสอบไฟล์
ls -la KPI-Sales-Dashboard/
```

---

## การแชร์กับคนอื่น

### 🔗 วิธีที่คนอื่นใช้งาน

#### วิธี 1: Clone (สำหรับนักพัฒนา)

```bash
git clone https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
cd KPI-Sales-Dashboard
# เปิด index.html ในเบราว์เซอร์
```

#### วิธี 2: Download ZIP (สำหรับผู้ใช้ทั่วไป)

1. ไปที่ https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard
2. คลิก **"Code"** → **"Download ZIP"**
3. แตกไฟล์
4. เปิด `index.html` ในเบราว์เซอร์

#### วิธี 3: GitHub Pages (ใช้ Online โดยไม่ต้อง Download)

**ตั้งค่า GitHub Pages**:

1. ไปที่ Repository Settings
2. ไปที่ **"Pages"**
3. ใน **"Source"** เลือก:
   - Branch: `main`
   - Folder: `/ (root)`
4. คลิก **"Save"**
5. รอ 1-2 นาที

แล้วเข้าไปที่:
```
https://YOUR_USERNAME.github.io/KPI-Sales-Dashboard/
```

### 📢 แชร์ลิงก์

**สำหรับการใช้ Online**:
```
https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard
```

**สำหรับ GitHub Pages** (ถ้าตั้งค่าแล้ว):
```
https://YOUR_USERNAME.github.io/KPI-Sales-Dashboard/
```

---

## 📝 การอัพเดตต่อไป

เมื่อคุณแก้ไขหรือเพิ่มฟีเจอร์ใหม่:

```bash
# ตรวจสอบการเปลี่ยนแปลง
git status

# เพิ่มไฟล์
git add .

# ยืนยัน
git commit -m "Feat: คำอธิบายการเปลี่ยนแปลง"

# อัพโหลด
git push origin main
```

---

## 🐛 Troubleshooting

### ❌ Error: "remote origin already exists"

```bash
# ลบ remote เก่า
git remote remove origin

# เพิ่มใหม่
git remote add origin https://github.com/YOUR_USERNAME/KPI-Sales-Dashboard.git
```

### ❌ Error: "fatal: not a git repository"

```bash
# เริ่มต้น git ใหม่
git init
git add .
git commit -m "Initial commit"
```

### ❌ Error: "Permission denied (publickey)"

ต้องตั้งค่า SSH Key:

```bash
# สร้าง SSH Key
ssh-keygen -t ed25519 -C "your.email@example.com"

# คัดลอก key
cat ~/.ssh/id_ed25519.pub

# เพิ่มไปที่ GitHub:
# Settings → SSH and GPG keys → New SSH key
# วาง Key และบันทึก
```

### ❌ ไฟล์ขนาดใหญ่ไม่อัพโหลด

GitHub มีขีดจำกัด 100MB ต่อไฟล์

ใช้ Git LFS:

```bash
git lfs install
git lfs track "*.psd"
git add .
git commit -m "Add large files"
git push
```

---

## 🎓 Tips และ Tricks

### 💡 การสร้าง Branch สำหรับ Development

```bash
# สร้าง branch ใหม่
git checkout -b develop

# ทำการเปลี่ยนแปลง
# ...

# Push branch ใหม่
git push origin develop
```

### 💡 การ Revert (ย้อนกลับ)

```bash
# ดู commit history
git log --oneline

# ย้อนกลับไป commit ใดๆ
git revert <commit-hash>
```

### 💡 การตั้งค่า CODEOWNERS

สร้างไฟล์ `.github/CODEOWNERS`:

```
* @YOUR_USERNAME

# หรือกำหนดเจ้าของ specific files
*.js @YOUR_USERNAME
*.css @YOUR_USERNAME
```

---

## 🎉 เสร็จแล้ว!

โปรเจ็กต์ของคุณอยู่บน GitHub แล้ว! 🎊

ตอนนี้:
- ✅ คนอื่นสามารถ Fork และ Contribute ได้
- ✅ คุณสามารถแชร์ลิงก์ได้
- ✅ โปรเจ็กต์ได้รับการ Backup
- ✅ สามารถติดตามการเปลี่ยนแปลงได้

---

## 📞 ติดต่อ Support

ถ้ามีปัญหา:
- 📖 ดู GitHub Help: https://docs.github.com
- 💬 สร้าง GitHub Issues ในโปรเจ็กต์ของคุณ
- 🔗 ดู Stack Overflow: https://stackoverflow.com

---

**Happy Sharing! 🚀**

**Last Updated**: 5 กุมภาพันธ์ 2026
